# 算法分析

---

## 1. HardCap 池模式性能问题（时间暴涨）

### 问题表现

HardCap 模式（`capacity_reference=5`）运行时间 **~140s**，Continuous 模式仅 **~3.5s**，相差约 **40 倍**。且 HardCap 的 HV 异常高（~190 vs ~104），呈近代价——时间与收敛质量同时偏离预期。

### 根因分析

**参数层面**：`capacity_reference=5` × 30 组件 = 总容量 150。但需服务 50 Agent × K=10 = 500 个组件槽位，供需比仅 30%。

**执行层面**（以 CR=5 实测为例）：

```
初始化后:
  解大小: min=0, max=10, mean=3.0（应为 10）
  耗尽组件: 30/30
  剩余容量: 0
```

- 初始化时前 ~15 个 Agent 耗尽所有组件容量，后续 ~35 个 Agent 只能拿到残缺解（甚至空解）
- `repair()` 仅做截断（Section 9.4），不补充缺失组件，因此 Agent 长期持有 0~4 个组件

**连锁效应**：

| 环节 | Continuous (正常) | HardCap (CR=5 耗尽) |
|------|-------------------|---------------------|
| shop 大小 | 5 | 0~2 |
| 每步候选数 | 8~15 (add + replace + release) | 1~2 (仅 release) |
| 每次 step 消耗 FE | ~100 | ~10 |
| 达到 5000 FE 所需迭代 | ~50 轮 | ~500 轮 |

每轮迭代有固定开销（50 Agent 遍历、预算分配、档案更新、rebirth 检查等），HardCap 需要 5~10 倍迭代次数来完成等量 FE，固定开销累积导致 40 倍时间差。

**收敛异常的原因**：当候选操作被限制为仅 release 时，Agent 的解不断收缩。小解在 MOSCSP 问题中天然倾向于高 quality/low cost 的极端点——因为少量高性价比组件的组合在目标空间中位于前沿边缘。这不是有效的 Pareto 搜索，而是容量耗尽导致的退化行为。

### 解决方案

1. **参数校准**：HardCap 模式下 `capacity_reference ≥ ceil(population_size × K / num_components)`。当前参数下需 ≥ 17（总容量 510 ≥ 500）。
2. **防御性告警**（已实现）：`mo_spps.py:178-190`，当 `total_capacity < total_demand` 时输出 WARNING，建议最小 capacity_reference。
3. **设计取舍**：HardCap 的"硬截断"语义决定了它在容量不足时会产生退化行为——这是模式特性，非代码缺陷。适度的容量压力（CR=12, 72%供需比）可创造选择压力；极端稀缺（CR=5, 30%）则破坏搜索动力学。

### 关键数据

| 模式 | CR | 总容量 | HV | |A| | JD | Ent(norm) | Time |
|------|-----|--------|------|------|------|------|------|
| Continuous | 5 | 150 | 104.1 | 33.5 | 0.685 | 0.951 | 3.5s |
| Continuous | 12 | 360 | 102.7 | 36.1 | 0.693 | 0.942 | 3.4s |
| HardCap | 5 | 150 | 189.6 | 27.0 | 0.778 | 0.496 | 140.5s |
| HardCap | 12 | 360 | 195.9 | 32.2 | 0.773 | 0.476 | 101.8s |

HardCap 的 HV 数值偏高伴随组件熵显著降低（0.48 vs 0.95）——收敛改善以多样性为代价，且时间成本极高。

---

## 2. Continuous 池 τ 参数与压力本质

### 问题

HardCap 的 HV 是 Continuous 的近 2 倍（196 vs 104），直觉反应是"Continuous 压力太温和"，尝试提高 τ 来增强压力。

### τ 参数机制

采样概率公式：`p_j ∝ (ε + φ_j)^τ`，其中 φ_j = 1 / (1 + u_j/Q_j)。

τ 是指数——τ 越大压力越**强**，而非越弱。热门组件（φ→0）在高 τ 下概率被指数级压缩：

| 场景 | τ=1 | τ=2 | τ=4 | τ=8 |
|------|-----|-----|-----|-----|
| 冷门(φ=0.9) vs 热门(φ=0.1) 的概率比 | **9×** | **70×** | **5,000×** | **2,700万×** |

τ=8 时 Agent 几乎被强制选择冷门组件，选择自由度归零。

### τ 扫描结果（10 seeds, CR=12）

**τ ≥ 1.0（高压区）**：

| τ | HV | JD | Ent(norm) | Gini | Time |
|---|------|------|------|------|------|
| 1.0 | 106.9 | 0.680 | 0.943 | 0.346 | 3.2s |
| 2.0 | 106.2 | 0.697 | 0.961 | 0.285 | 3.0s |
| 4.0 | 97.9 | 0.726 | 0.979 | 0.213 | 2.8s |
| 8.0 | 91.9 | 0.738 | 0.992 | 0.130 | 2.5s |

τ 每翻一倍，HV 持续下降，Gini 持续降低。τ=8 时 Gini 仅 0.13——组件使用近乎完全均匀，Agent 被迫使用不合适的组件，收敛彻底崩溃。

**τ ≤ 1.0（低压区）**：

| τ | HV | JD | Ent(norm) | Gini | Time |
|---|------|------|------|------|------|
| 0.00 | **110.2** | 0.659 | 0.914 | 0.409 | 3.5s |
| 0.25 | 110.0 | 0.675 | 0.926 | 0.383 | 3.5s |
| 0.50 | 109.0 | 0.688 | 0.931 | 0.377 | 3.4s |
| 0.75 | 105.7 | 0.668 | 0.936 | 0.360 | 3.2s |
| 1.00 | 106.9 | 0.680 | 0.943 | 0.346 | 3.2s |
| mode=none | 109.4 | 0.674 | 0.922 | 0.398 | 3.1s |

τ=0 与 mode=none 的 HV 仅差不到 1%（110.2 vs 109.4），数学上源自 `(ε + φ)^0 = 1`——池压力项消失，退化为均匀采样。τ ∈ [0, 0.5] 区间 HV 稳定在 109~110，说明**低压或无压时收敛最好**。

### 结论

1. **τ=0 时池压力消失，等价于 mode=none**。数学上 `(ε + φ)^0 = 1`，所有组件等权。实测 HV 差异 < 1%（110.2 vs 109.4），在噪声范围内完全一致。
2. **τ 越低收敛越好**。τ ∈ [0, 0.5] 时 HV 稳定在 109~110，τ ≥ 1 后 HV 单调下降至 τ=8 的 91.9。池压力的主要作用是调节组件使用均匀度（Gini），而非提升收敛质量。
3. **Continuous 池的默认 τ=1.0 处于折中点**：Gini=0.35 提供了适度的去重压力，HV 仅比最优 τ=0 低约 3%（106.9 vs 110.2），同时保留了多样性优势。
4. **HardCap 的 HV 优势不是来自"更强的压力"**。两者机制根本不同：
   - Continuous：（ε + φ_j）^τ —— 任何组件永远可采样（ε > 0），压力只调节概率
   - HardCap：_remaining[j] > 0 —— 容量耗尽后组件概率归零，硬截断迫使搜索在受限子集中进行组合优化
5. **消融实验中的呈现标准**：HardCap vs Continuous 对比必须同时展示 HV 和多样性指标（JD, Ent, Gini），避免以 HV 单维评判"优劣"。

### 关键数据

| 模式 | HV | JD | Ent(norm) | Gini | Time |
|------|------|------|------|------|------|
| Continuous (τ=0, CR=12) | 110.2 | 0.659 | 0.914 | 0.409 | 3.5s |
| Continuous (τ=1, CR=12) | 106.9 | 0.680 | 0.943 | 0.346 | 3.2s |
| Continuous (τ=8, CR=12) | 91.9 | 0.738 | 0.992 | 0.130 | 2.5s |
| mode=none | 109.4 | 0.674 | 0.922 | 0.398 | 3.1s |
| HardCap (CR=12) | 195.9 | 0.773 | 0.476 | 0.326 | 101.8s |

---
